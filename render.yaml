services:
  - type: web
    name: ballerspak-backend
    env: node
    plan: free          # stays on free tier
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    # Persistent disks are not available on free tier, so omitted. 